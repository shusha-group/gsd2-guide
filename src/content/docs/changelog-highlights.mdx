---
title: Changelog Highlights
description: Scannable per-release summaries for solo builders — the key additions, changes, and fixes in each GSD 2 release.
---

import highlights from '../../../content/generated/changelog-highlights.json';

Quick-scan release summaries focused on what matters for solo builders. Each entry surfaces the most notable addition, change, and fix per release. For the full list of every change, see the [Changelog](/gsd2-guide/changelog/).

<div class="highlights-list">
{highlights.map((release) => {
  const date = new Date(release.published_at).toLocaleDateString('en-US', {
    year: 'numeric', month: 'short', day: 'numeric',
  });
  return (
    <div class="highlight-entry" key={release.tag_name}>
      <div class="highlight-header">
        <a href={release.html_url} target="_blank" rel="noopener noreferrer" class="highlight-version">{release.tag_name}</a>
        <span class="highlight-date">{date}</span>
      </div>
      <p class="highlight-summary">{release.summary}</p>
    </div>
  );
})}
</div>

<style>{`
  .highlights-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1.5rem;
  }
  .highlight-entry {
    border-left: 3px solid var(--sl-color-accent);
    padding: 0.75rem 1rem;
    background: var(--sl-color-bg-sidebar);
    border-radius: 0 0.375rem 0.375rem 0;
  }
  .highlight-header {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    margin-bottom: 0.375rem;
  }
  .highlight-version {
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    color: var(--sl-color-accent);
  }
  .highlight-version:hover {
    text-decoration: underline;
  }
  .highlight-date {
    font-size: 0.8rem;
    color: var(--sl-color-gray-3);
  }
  .highlight-summary {
    margin: 0;
    font-size: 0.875rem;
    line-height: 1.55;
    color: var(--sl-color-text);
  }
`}</style>
