---
title: "Extensions"
description: "22 GSD extensions — tools, capabilities, and integrations built into the pi agent."
---

import extensions from '../../../../content/generated/extensions.json';
import ReferenceCard from '../../../../src/components/ReferenceCard.astro';
import ReferenceGrid from '../../../../src/components/ReferenceGrid.astro';
import ToolList from '../../../../src/components/ToolList.astro';

export const sorted = [...extensions].sort((a, b) => b.tools.length - a.tools.length);
export const categories = ["Has Tools", "No Tools"];

Extensions are built-in capabilities bundled with the pi agent. Each extension either registers tools the agent can call directly, or provides hooks, commands, and stream-level guardrails that operate transparently without direct tool calls.

22 extensions are active by default. They fall into a few broad categories:

- **Interaction** — collect user input and secrets through masked, paged TUI flows; voice input support (`ask-user-questions`, `get-secrets-from-user`, `voice`)
- **Shell & process management** — run commands asynchronously, manage persistent background processes, and track their lifecycle (`async-jobs`, `bg-shell`)
- **Browser automation** — full Playwright-powered browser control with 61 tools covering navigation, interaction, assertions, tracing, network inspection, and state save/restore (`browser-tools`)
- **Search & documentation** — web search via Brave with combined search-and-read, page fetching, Google Search via Gemini grounding, and up-to-date library docs via Context7 (`search-the-web`, `google-search`, `context7`)
- **macOS automation** — Accessibility API, window management, and screen capture for native app control (`mac-tools`)
- **Model providers** — delegate inference to locally-installed Claude Code CLI or on-device Ollama models (`claude-code-cli`, `ollama`)
- **Infrastructure** — native MCP server client, subagent spawning (single/parallel/chain modes), GSD database and planning operations, configuration discovery, slash-command routing, and terminal multiplexing (`mcp-client`, `subagent`, `gsd`, `universal-config`, `slash-commands`, `cmux`)
- **Guardrails & hooks** — zero-cost stream-level regex guardrails, AWS credential auto-refresh, GitHub sync, and remote configuration validation (`ttsr`, `aws-auth`, `github-sync`, `remote-questions`)

Cards below are sorted by tool count — extensions with more tools appear first. Click any card to expand and see its registered tools.

<ReferenceGrid categories={categories}>
  {sorted.map((ext) => (
    <ReferenceCard
      title={ext.name}
      subtitle={ext.description || "No description available"}
      badge={ext.tools.length > 0 ? ext.tools.length + (ext.tools.length === 1 ? " tool" : " tools") : undefined}
      category={ext.tools.length > 0 ? "Has Tools" : "No Tools"}
    >
      {ext.tools.length > 0 ? (
        <ToolList tools={ext.tools} />
      ) : (
        <p><em>This extension uses hooks or commands instead of registered tools.</em></p>
      )}
    </ReferenceCard>
  ))}
</ReferenceGrid>
