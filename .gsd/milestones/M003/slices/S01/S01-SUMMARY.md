Detection-only pipeline that maps all 42 authored doc pages to their gsd-pi source dependencies and identifies which pages are stale after source changes. Produces page-source-map.json (42 entries, 485 deps), detectChanges() and resolveStalePages() in scripts/lib/diff-sources.mjs, previous-manifest.json snapshot, and stale-pages.json boundary contract. Wired into npm run update between extract and build steps. 21 tests pass (9 page-map + 12 diff-sources). Requirements R034, R035, R036, R037, R046 validated.