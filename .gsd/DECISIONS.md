# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D041 | M003 S01 T03 | architecture | S01→S02/S03/S04 boundary contract format for stale-pages.json | JSON file at `content/generated/stale-pages.json` with shape: `{ changedFiles[], addedFiles[], removedFiles[], stalePages[], reasons: { page: [files] }, timestamp }` — or `{ firstRun: true, stalePages: [], timestamp }` for initial runs | Downstream slices need a stable, well-defined interface to consume diff results. A JSON file is simple, inspectable, and doesn't require in-process coupling. The reasons object maps page→triggering-files so S02 can log why each page needs regeneration. | Yes |
| D042 | M003/S01 | convention | Boundary contract format for pipeline stage output | stale-pages.json written on every pipeline run (including first-run and zero-stale cases) with fields: changedFiles, addedFiles, removedFiles, stalePages, reasons (object), timestamp | Always writing the file means downstream slices (S02/S03/S04) can unconditionally read it without checking existence. Including all diff categories in one file gives each downstream consumer what it needs: S02 reads stalePages, S03 reads addedFiles/removedFiles, S04 orchestrates all. | Yes |
| D043 | M003/S01 | convention | Page-source-map key format and dependency paths | Page keys use relative-to-content format (e.g. "commands/auto.mdx") without src/content/docs prefix. Source deps use repo-relative paths matching manifest.json keys (e.g. "src/resources/extensions/gsd/auto.ts"). Reference pages dynamically pull all matching files from manifest rather than hardcoding. | Page keys match the content collection slug format for easy lookup. Repo-relative source paths match manifest.json for direct hash comparison. Dynamic reference page deps avoid stale hardcoded lists when new skills/agents/extensions are added. | Yes |
| D044 | M003/S01 | convention | Boundary contract format for pipeline stage output | stale-pages.json written on every pipeline run (including first-run and zero-stale cases) with fields: changedFiles, addedFiles, removedFiles, stalePages, reasons (object), timestamp | Always writing the file means downstream slices (S02/S03/S04) can unconditionally read it without checking existence. Including all diff categories in one file gives each downstream consumer what it needs: S02 reads stalePages, S03 reads addedFiles/removedFiles, S04 orchestrates all. | Yes |
| D045 | M003/S02 | convention | Quality exemplar strategy for LLM page regeneration | capture.mdx hardcoded as exemplar page — included in every system prompt as the canonical quality reference | capture.mdx is a representative command deep-dive with all 6 required sections, Mermaid diagrams, file tables, and relative links. Including it in the system prompt gives Claude a concrete quality target rather than relying on abstract rules alone. Hardcoding avoids the complexity of exemplar selection logic. | Yes — if a better exemplar emerges or per-page-type exemplars are needed |
| D046 | M003/S02 | arch | Dependency injection pattern for Anthropic SDK in regeneration module | Mock client injected via options.client parameter — dynamic import of @anthropic-ai/sdk only when API key is present | Avoids SDK module mocking complexity in tests. Dynamic import means no top-level dependency on the SDK — the module loads and exports cleanly even without the package installed. Tests inject a fake client object directly via options.client, making all code paths testable without network access or environment variables. | Yes |
| D047 | M003/S03 | convention | Sidebar entry matching strategy for add/remove | Match by link pattern (/commands/{slug}/) rather than label text | Label text is ambiguous — "/gsd" and "/gsd auto" could match incorrectly. Link patterns are unique identifiers since each command has a distinct URL slug. This avoids false matches and makes removal reliable. | Yes |
| D048 | M003/S03 | convention | When to update sidebar and page-source-map during createNewPages | Only update sidebar/map when regeneration succeeds (not when skipped or errored) | Prevents orphaned metadata entries (sidebar link to nonexistent page, map entry for page that was never created). If regeneration fails or is skipped, there's no page to reference — adding metadata would cause broken links or stale data. | Yes |
| D009 | M001/S01 | arch | Content extraction script architecture | Single ESM Node.js script (`scripts/extract.mjs`) with modular extractors (`extract-local.mjs`, `extract-github-docs.mjs`, `extract-releases.mjs`, `extract-commands.mjs`, `manifest.mjs`) | Modular structure allows parallel execution (local + GitHub phases), independent testing, and clean separation of concerns. ESM enables top-level await and native fetch(). GitHub content uses tarball endpoint (1 API call for all 126 docs) instead of per-file fetches to stay well within the 60/hour unauthenticated rate limit. | Yes |
| D010 | M001/S01 | convention | Test framework for extraction pipeline | Node.js built-in test runner (`node:test` + `node:assert`) with `tests/extract.test.mjs` | Zero dependencies — built into Node.js 20+. The extraction pipeline is straightforward enough that structural assertions (count checks, field existence, file existence) are sufficient. No need for Jest/Vitest overhead for a build script. | Yes |
| D011 | M001/S01 | convention | Extraction output format and location | Pretty-printed JSON files + markdown in content/generated/ directory | JSON is directly consumable by Astro content collections and quick-reference card components. content/generated/ is gitignored — always regenerated from source. Manifest with SHA hashes enables downstream diff tracking. | Yes — if Astro content collections need a different format |
| D012 | M001/S01 | arch | Extension tool extraction strategy | Recursive TS file scanning with dual regex patterns (pi.registerTool + ToolDefinition factory returns) | Not all extensions use direct pi.registerTool() calls — some use factory functions returning ToolDefinition objects. Dual-pattern approach captures all tools including async-jobs' createAsyncBashTool pattern. | No |
| D013 | M001/S01 | convention | Extraction output format and location | Pretty-printed JSON files + markdown in content/generated/ directory | JSON is directly consumable by Astro content collections and quick-reference card components. content/generated/ is gitignored — always regenerated from source. Manifest with SHA hashes enables downstream diff tracking. | Yes — if Astro content collections need a different format |
| D014 | M001/S02 | arch | Content bridge between S01 extraction output and Starlight content directory | Prebuild script (`scripts/prebuild.mjs`) that copies `content/generated/docs/` → `src/content/docs/` with frontmatter injection | Starlight's `docsLoader()` reads only from `src/content/docs/` — cannot be pointed at `content/generated/docs/`. S01 extracted docs have no YAML frontmatter (start with `# Title`), but Starlight requires at minimum a `title` field. A prebuild script is the cleanest solution: it extracts the first `# Heading` as the title, injects YAML frontmatter, strips the duplicate heading, and preserves directory structure. Runs before `astro dev` and `astro build` via package.json scripts. Tracks generated files via manifest to avoid deleting hand-authored placeholder pages. | Yes — if Starlight adds configurable content directory support |
| D015 | M001/S02 | convention | Placeholder content strategy to avoid collisions with generated content | Placeholder pages in `src/content/docs/placeholder/` directory, removed when real content arrives in S03/S04 | Placeholder pages created in S02 must coexist with S01 generated content that the prebuild script copies into `src/content/docs/`. Using a dedicated `placeholder/` subdirectory prevents filename collisions. The prebuild script tracks its own generated files via a manifest and never touches the placeholder directory. When S03/S04 create real content pages, the placeholder directory can be cleanly removed. | No — convention, not architecture |
| D016 | M001/S02/T01 | library | How to resolve @astrojs/internal-helpers version conflict between Astro 6 and starlight-client-mermaid | npm overrides: force @astrojs/internal-helpers to 0.8.0 in package.json | @pasqal-io/starlight-client-mermaid@0.1.0 depends on @astrojs/markdown-remark@6.x which pulls in @astrojs/internal-helpers@0.7.6. Astro 6 requires 0.8.0 for collapseDuplicateLeadingSlashes export. npm hoists the older version, breaking the build. The override forces the correct version globally without patching the mermaid plugin. | Yes — when @pasqal-io/starlight-client-mermaid updates to support Astro 6 natively |
| D017 | M001/S02/T03 | convention | Prebuild content bridge strategy | Manifest-based prebuild (scripts/prebuild.mjs) with .generated-manifest.json tracking | Prebuild copies S01 extracted docs into Starlight content dir with injected YAML frontmatter. Manifest tracks generated files so cleanup only removes its own files, never placeholder pages. Idempotent re-runs. | Yes — pipeline may evolve in S06 |
| D018 | M001/S02/T01 | library | npm override for @astrojs/internal-helpers version conflict | Add "overrides": { "@astrojs/internal-helpers": "0.8.0" } to package.json | @pasqal-io/starlight-client-mermaid@0.1.0 pulls @astrojs/internal-helpers@0.7.6 via its @astrojs/markdown-remark dep, but Astro 6 needs 0.8.0 (for collapseDuplicateLeadingSlashes). npm hoists 0.7.6, breaking the build. | Yes — when starlight-client-mermaid updates its deps |
| D019 | M001/S02/T03 | convention | Prebuild content bridge strategy | Manifest-based prebuild (scripts/prebuild.mjs) with .generated-manifest.json tracking | Prebuild copies S01 extracted docs into Starlight content dir with injected YAML frontmatter. Manifest tracks generated files so cleanup only removes its own files, never placeholder pages. Idempotent re-runs. | Yes — pipeline may evolve in S06 |
| D020 | M001/S03 | convention | MDX import pattern for generated JSON data | Relative path imports (../../../../content/generated/*.json) with export const for computed data | MDX files in src/content/docs/reference/ must traverse up 4 levels to reach content/generated/. Using export const between frontmatter and JSX is the correct Astro/MDX pattern for derived data (e.g., category extraction, filtering). | Yes |
| D021 | M001/S03 | convention | Reference card expand/collapse mechanism | Native HTML <details>/<summary> with CSS-only animation | Zero client JavaScript for expand/collapse. Works without JS. Custom ::before triangle indicator for cross-browser consistency. Aligns with Astro's zero-JS-by-default philosophy. | Yes |
| D022 | M001/S03 | convention | MDX import pattern for generated JSON data | Relative path imports (../../../../content/generated/*.json) with export const for computed data | MDX files in src/content/docs/reference/ must traverse up 4 levels to reach content/generated/. Using export const between frontmatter and JSX is the correct Astro/MDX pattern for derived data (e.g., category extraction, filtering). | Yes |
| D023 | M001/S04 | convention | Sidebar organization for root-level doc pages | Explicit sidebar entries in astro.config.mjs grouped into "Guides" (15 pages) and "Architecture" (4 pages), with autogenerate for subdirectory-based content groups | Root-level .md files are not picked up by Starlight's autogenerate (which only reads subdirectories). Moving files into subdirectories during prebuild would complicate the pipeline and break internal links. Explicit sidebar entries are simpler (only 19 entries), give precise control over ordering and grouping, and avoid file path changes. Subdirectory groups (what-is-pi, building-coding-agents, etc.) continue using autogenerate since their numbered filenames sort naturally. | Yes |
| D024 | M001/S04 | convention | Sidebar organization for documentation pages | Root-level pages split into explicit Guides (15) and Architecture (4) groups; subdirectories use autogenerate | Root-level pages have no directory structure for autogenerate to use. Explicit entries allow logical reading order. Subdirectories (what-is-pi, building-coding-agents, etc.) have numbered files that autogenerate handles correctly. | Yes — if more root-level pages are added |
| D025 | M001/S04 | convention | Sidebar organization for documentation pages | Root-level pages split into explicit Guides (15) and Architecture (4) groups; subdirectories use autogenerate | Root-level pages have no directory structure for autogenerate to use. Explicit entries allow logical reading order. Subdirectories (what-is-pi, building-coding-agents, etc.) have numbered files that autogenerate handles correctly. | Yes — if more root-level pages are added |
| D001 | M001 | arch | Site framework | Astro + Starlight | Purpose-built for doc sites, zero client JS, built-in Pagefind search, GitHub Pages deployment support, content collections | No |
| D002 | M001 | arch | Hosting platform | GitHub Pages | Free, CDN-backed, git push to deploy, source repo already on GitHub | Yes — if custom domain or server features needed |
| D003 | M001 | arch | Content source of truth | Installed npm package + GitHub repo | npm package has behavioral truth (prompts, templates, skills), GitHub repo has narrative docs. Both needed. | No |
| D004 | M001 | scope | Version display strategy | Latest + changelog (no per-version snapshots) | 6+ releases/day makes per-version snapshots impractical. Latest docs + browsable changelog covers the need. | Yes — if release velocity slows |
| D005 | M001 | scope | Update trigger | Manual one-command trigger | User wants control over when docs update. CI/CD auto-trigger deferred. | Yes — can add CI/CD later |
| D006 | M001 | convention | Design aesthetic | Terminal-native dark with visual aids | Dark-mode-first, tight typography, code-heavy foundation. Diagrams and illustrations for vibe-coder accessibility. "Craft feel" per user emphasis. | No |
| D007 | M001 | convention | Quick-reference format | Cheat-sheet cards (searchable, filterable, expandable) | Fastest lookup for developers coding alongside GSD. Interactive playground deferred. | Yes — if users want more interactivity |
| D008 | M001 | arch | AI consumption strategy | Well-structured semantic HTML + sitemap | Good structure is sufficient for AI parsing. No dedicated JSON API or llms.txt needed. | Yes — if AI consumption becomes primary use case |
| D026 | M001/S06 | arch | GitHub Actions deployment concurrency — cancel-in-progress: false | Let in-progress deployments complete rather than cancelling on newer pushes | Cancelling a deployment mid-flight could leave GitHub Pages in a partial state. Completing the current deploy before starting the next ensures the site is always in a consistent state. | Yes |
| D027 | M001/S06 | arch | Link checker scope — check only `<a>` tags, not `<link>` tags | Only scan `<a>` href attributes in dist/ HTML | Starlight injects `<link>` tags for favicon.svg and sitemap-index.xml that don't map to dist/ files. Checking `<link>` tags produces 268 false positives (134 pages × 2). `<a>` tags are the user-navigable links that matter for broken link detection. | Yes |
| D028 | M002 | scope | Content focus — GSD-only, remove pi/agent content | Remove 101 pages of generic pi/agent/extension/TUI content (What Is Pi, Building Coding Agents, Context and Hooks, Extending Pi, Pi UI/TUI, Proposals) from the site | User explicitly wants GSD-focused documentation. Generic pi content is noise for the target audience — developers learning to use GSD, not developers building pi extensions. | No |
| D029 | M002 | convention | Command documentation approach — authored from source study, not extracted | Hand-author command deep-dives by studying GSD source code (commands.ts, prompts/, auto-dispatch.ts). Use explanations, examples, and Mermaid visuals — not prompt text regurgitation. | User explicitly said "don't regurgitate the prompt back." The goal is teaching content that helps users understand what GSD does and why, not a dump of internal implementation. | No |
| D030 | M002 | convention | Walkthrough uses a real project example, not conceptual | End-to-end walkthrough follows a concrete example project showing actual .gsd/ artifacts at each phase | A real example grounds the walkthrough in tangible artifacts. Users can compare what they see in their own projects against the walkthrough. | No |
| D031 | M002 | scope | Core recipes now, advanced recipes in M003 | M002 covers 6 core workflow recipes. Advanced patterns (parallel orchestration, worktree strategies, custom hooks, headless/CI, cost optimization) deferred to M003. | User explicitly deferred advanced recipes: "we will add advanced in next milestone." | No |
| D032 | M002/S01/T02 | convention | Walkthrough example project identity | Cookmate (recipe-sharing web app) as the concrete example project threading through the end-to-end walkthrough | Complex enough to show real milestone/slice/task decomposition (auth, recipe CRUD, search, social features), simple enough domain that readers understand without explanation. All future references to "the example project" in command deep-dives and recipes should use or be consistent with Cookmate. | Yes |
| D033 | M002/S03/T03 | convention | Special topic reference page structure | Reference-table format for keyboard-shortcuts, cli-flags, and headless pages — omit inapplicable sections (Usage, What Files It Touches) and use flag/shortcut tables with descriptions and examples | These pages document cross-cutting concerns, not individual command lifecycles. The standard command template (What It Does → Usage → How It Works → What Files It Touches → Examples) doesn't fit. Reference tables are the natural format for "here are all the flags/shortcuts" content. Headless uses a Mermaid diagram since it has a real process lifecycle (spawn → events → exit codes). | Yes |
| D034 | M002/S03/T03 | convention | Special topic reference page structure | Reference-table format for keyboard-shortcuts, cli-flags, and headless pages — omit inapplicable sections (Usage, What Files It Touches) and use flag/shortcut tables with descriptions and examples | These pages document cross-cutting concerns, not individual command lifecycles. The standard command template (What It Does → Usage → How It Works → What Files It Touches → Examples) doesn't fit. Reference tables are the natural format for "here are all the flags/shortcuts" content. Headless uses a Mermaid diagram since it has a real process lifecycle (spawn → events → exit codes). | Yes |
| D035 | M003 | arch | Doc regeneration approach | LLM-powered (Claude API) over deterministic code-to-docs generator | M002 proved Claude Code can produce teaching-quality documentation from source code. A deterministic generator would be limited to structural extraction — no editorial judgment, no explanations, no diagrams. The API approach preserves M002 quality. | Yes — if API costs become prohibitive |
| D036 | M003 | arch | Regeneration trigger | Part of `npm run update` (manual), not CI auto-trigger | User wants control over when regeneration runs and wants to observe the output. CI auto-trigger deferred to later. | Yes — can add CI trigger later |
| D037 | M003 | arch | Regeneration scope | Only changed pages, not full regeneration | Surgical regeneration based on source diff keeps the pipeline fast and cheap. Touching all 42 pages on every update would be wasteful. | Yes — full regen available as fallback |
| D038 | M003 | arch | Change detection method | Package file diff (hash comparison of installed dist/ files) | Direct file comparison is reliable and works offline. Changelog-based inference depends on release notes being accurate and complete, which isn't guaranteed. | No |
| D039 | M003 | arch | Source-to-page mapping approach | Explicit manifest (page-source-map.json) over convention-based inference | Explicit mapping is precise — each page declares exactly which source files it depends on. Convention-based inference (guessing from file names) would miss cross-cutting dependencies and produce false negatives. | Yes — if mapping maintenance becomes burdensome |
| D040 | M003 | convention | Regenerated page review model | Auto-commit, no human review gate | Same trust level as M002 — the agent wrote those pages and they were deployed directly. Adding a review gate for automated updates would slow the pipeline and contradict the zero-manual-intervention goal. | Yes — if quality issues emerge |
| D049 | M003/S04 | convention | How to handle broken links from ReleaseEntry.astro after manage-pages removes a command | Fix at the source (remove stale slugs from commandSlugs map in ReleaseEntry.astro) rather than adding an allowlist to check-links.mjs | Keeping the link checker strict and honest prevents broken links from accumulating. The manage-pages removal workflow should include slug cleanup as part of its contract — the KNOWLEDGE.md entry documents this as a required follow-up step. | Yes — if the number of stale slug cleanups becomes burdensome, automate it in manage-pages.mjs |
| D050 | M004 | arch | Regeneration engine — claude -p over @anthropic-ai/sdk | Replace API-based regeneration with `claude -p` subprocess invocation | The bare API approach sends source files in a prompt and gets back text with no ability to read additional files, check links, or iterate. `claude -p` gives Claude Code full tool access — the same capability that wrote the original M002 documentation. Quality and accuracy should match the original creation process. | No — this supersedes D035 which chose API over deterministic generator; the real choice is Claude Code vs bare API |
| D051 | M004 | arch | Local-only regeneration — no GitHub Actions CI trigger | Regeneration runs locally via `npm run update` (triggered by "update gsd-guide"), not in GitHub Actions | User explicitly chose local Claude Code over CI-based approach. No API key required — uses existing Claude Code subscription. User controls when regeneration runs and can observe output. | Yes — CI auto-trigger can be added later if desired |
| D052 | M004/S01 | convention | Subprocess mock testing pattern for claude -p tests | Shell script fixture (mock-claude.sh) controlled by env vars (MOCK_EXIT_CODE, MOCK_WRITE_PATH, MOCK_BAD_FRONTMATTER, MOCK_CAPTURE_STDIN) replaces SDK client mock injection (options.client) | D046's options.client injection no longer exists after the SDK removal. The mock script simulates all claude CLI behaviors (--version, stream-json output, file writing, error exit codes) via env vars, enabling deterministic testing of all result shapes without the real CLI. Stdin capture enables indirect verification of unexported prompt construction functions. | Yes |
| D053 | M004/S02 | convention | Pipeline behavior when some but not all page regenerations fail | Tolerate partial regeneration failures — pipeline continues and builds with existing content for failed pages, only fails if ALL pages fail | LLM subprocess calls are inherently flaky (API timeouts, claude -p occasionally deleting files). Failing the entire pipeline because one page failed would make the update workflow unreliable. Building with existing content for failed pages is better than building nothing. The pipeline logs ✗ for failed pages so the operator can retry individually. | Yes — if quality requirements demand all-or-nothing regeneration |
| D054 | M004/S02 | convention | What counts as "pipeline time" for the R055 fast-path <15s target | Pipeline-logic time (8.7s) is the R055 metric, not total wall-clock time (20.7s) which includes `npm i -g gsd-pi@latest` network fetch (~12s) | The `npm i -g gsd-pi@latest` step is a network fetch whose latency varies with npm registry speed and is outside the pipeline's control. The meaningful metric is the pipeline logic: extract → detect-stale → regenerate(skip) → build → link-check → stamp. This completed in 8.7s, well under 15s. | Yes — if the npm fetch becomes cacheable or skippable |
| D055 | M005 | arch | Prompt metadata strategy — `prompts.json` file vs on-the-fly derivation | Extract `content/generated/prompts.json` at build time containing name, slug, group, variables, pipelinePosition, usedByCommands for all 32 prompts | Claude reading `auto-prompts.ts` once during extraction and producing a structured JSON is cheaper and more consistent than re-deriving the same facts in 32 separate page-generation runs. The JSON is a stable contract that S02 (scaffold), S03 (generation), S04 (backlinks), and S05 (pipeline) all consume independently. | Yes — if prompts.json proves too brittle to maintain |
| D056 | M005 | convention | Prompt page structure — 4 sections, explanation-first | What It Does → Pipeline Position (Mermaid) → Variables (table) → Used By (links) | Matches the spirit of D029 (explanation, not source dump). The Mermaid diagram is the distinctive visual — it comes second, after establishing what the prompt is for, so the diagram has context. Variable table third keeps the explanatory prose upfront. | No |
| D057 | M005 | convention | Prompt grouping taxonomy — 4 canonical groups | Auto-mode pipeline (10), Guided variants (8), Commands (13), Foundation (1) | Groups reflect the structural role of each prompt in the system, not surface-level naming. Auto-mode pipeline = dispatched by auto-prompts.ts loop. Guided variants = interactive wrappers around pipeline prompts. Commands = invoked directly by user-facing slash commands. Foundation = system.md, the persona prompt injected into every session. | Yes — if new prompt categories emerge |
| D058 | M005 | convention | page-source-map key format for prompt pages | `prompts/{slug}.mdx` — consistent with `commands/{slug}.mdx` pattern | Uniform key format across all authored page types makes the map easier to reason about and keeps the stale detection logic simple — one function handles both. | No |
| D059 | M005/S03 | convention | Mermaid diagram scope — 5-7 nodes, not the full 10-stage pipeline | Show current prompt + 2-3 immediate neighbors on each side rather than the complete auto-mode loop | A 10-node pipeline diagram is wide and hard to read at documentation page widths. The 5-7 node view is self-contained: it shows what comes before and after without overwhelming the reader. Users who need the full picture consult the auto-mode walkthrough page. | Yes — if users request full-pipeline diagrams |
| D060 | M005/S03 | convention | Guided variant diagram uses session model, not auto-mode pipeline | Fixed 5-node pattern (/gsd → select-unit → guided-{type} → user-interaction → artifact-written) for all 8 guided variant pages | Guided sessions are dispatched directly by /gsd — they don't follow the auto-mode sequential stage order (research → plan → execute → complete). Showing the auto-mode pipeline for guided variants would be factually incorrect. The session model is the accurate representation. | No |
| D061 | M005/S03 | convention | MDX template variable escaping — backtick wrapping | Wrap GSD prompt template syntax ({{variable}}) in backtick code spans in MDX prose | MDX parses {{foo}} as {foo} JSX expression at render time, producing a ReferenceError at build time. Backtick wrapping renders the literal as inline code and avoids JSX evaluation. HTML entities (&lt;&#123;) work but are less readable. | No |
| D062 | M005/S04 | convention | Prompt link description truncation strategy — em-dash split first | Truncate prompt link descriptions by splitting on ' — ' (em-dash separator) first, then '. ' period-space, then hard-cap at 80 chars | Prompt description fields in prompts.json use the format "Short phrase — longer explanation". Splitting on em-dash yields the punchy lead phrase. Fallback to period-split handles descriptions without em-dash. Hard cap prevents runaway long lines. Two-level fallback was needed because some prompts (rewrite-docs, run-uat) had no em-dash separator. | Yes — if description format changes in prompts.json |
| D063 | M005/S04 | convention | Backlink insertion — immediately before ## Related Commands anchor | Insert `## Prompts Used` section immediately before the `## Related Commands` heading, not at end of file | Command pages consistently end with `## Related Commands`. Using that anchor keeps Prompts Used as the penultimate section, which is logically correct (prompts are more internal/technical than related commands). Script fails gracefully (logs SKIP) if the anchor is absent, rather than appending to an arbitrary location. | No |
| D064 | M005/S05 | convention | Sidebar sub-group insertion detection strategy in manage-pages.mjs for prompt entries | Items-array bracket detection: find `items: [` after the label line, then match closing `],` at the same indentation level, insert alphabetically | Hard-coding line offsets is brittle — any sidebar edit breaks the insertion. The bracket-matching approach is resilient to arbitrary sidebar reformatting as long as the `items: [` / `],` structure is consistent. 16-space indentation for prompt entries (2-level nesting: Prompts → sub-group → entry) vs 12-space for command entries (1-level). | Yes — if Prettier or a linter reformats astro.config.mjs |
| D065 | M005/S05 | arch | Which field in page-versions.json drives staleness detection | Stale detection compares `manifest.files[dep]` SHA against `recorded.deps[dep]` SHA in page-versions.json; headSha is metadata only | headSha is the git HEAD commit when the page was stamped — it's informational metadata. The actual staleness signal is whether the source file's content hash changed, which is tracked per-dependency in `recorded.deps`. This was discovered during stale simulation when tampering headSha produced no stale flag — dep SHA tampering was required instead. | No — this is the core mechanism; changing it would require rethinking the staleness model |
| D066 | M005/S05 | convention | How regenerate-page.mjs selects which system prompt and exemplar to use per page type | Page-type dispatch in `regeneratePage()`: `pagePath.startsWith('prompts/')` → `buildPromptSystemPrompt()` + `execute-task.mdx` exemplar; else → `buildSystemPrompt()` + `capture.mdx` exemplar | Prompt pages and command pages have fundamentally different structures (4-section vs 6-section). A single system prompt cannot produce both correctly. Path prefix dispatch is the simplest extension point — adding a new page type in future only requires adding another `startsWith` branch. | Yes — if more page types are added that need distinct system prompts |
